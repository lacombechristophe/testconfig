{
  "name": "diskoov-configurateur",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "test": "node --test tests/*.test.js",
    "typecheck": "tsc --noEmit --target ES2020 --module commonjs api/send-email.ts"
  },
  "dependencies": {
    "@vercel/kv": "^1.0.0",
    "resend": "^3.2.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@vercel/node": "^3.0.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": "20.x"
  }
}
